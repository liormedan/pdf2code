# Test corpus, harnesses and planning notes: none of them are served, and the PDFs
# alone are tens of megabytes. scripts/ stays — prebuild runs sync-pdf-assets.mjs.
fixtures
bakeoff
docs
out
.next-build

# The explainer film. Its own project, its own node_modules, and nothing in it is
# served — the finished video ships as public/explainer.mp4, which is committed.
video

# The desktop application. Its own project, its own toolchain, and nothing in it is
# served by the site — it ships as an installer, not as a route.
desktop

# pdf2code

**המוצר הפעיל הוא הדסקטופ, ב-`desktop/`.** אחזקת האתר (`app/`, `components/`, `lib/`) ירדה בעדיפות בהכרעת הנהלה מ-5.9.2026.

## שני מסמכים לקרוא לפני שנוגעים בקוד

| מסמך | מתי |
| --- | --- |
| **[desktop/delegation.md](desktop/delegation.md)** | מי מכריע ומי מבצע, מה מותר לי בלי לשאול, ותיאור הפרויקט. **התחנה הראשונה** |
| **[desktop/conventions.md](desktop/conventions.md)** | איך כותבים כאן — הערות, שמות, בדיקות, כישלונות, אבטחה, קומיטים |

הנימוקים: [architecture.md](desktop/architecture.md) · [decisions.md](desktop/decisions.md) · [built.md](desktop/built.md) · [roadmap.md](desktop/roadmap.md)

## שמונה כללים שגוברים על נוחות

1. **אמת הריפו גוברת על הקשר זכור.** מה שזכור סותר את הקוד — הקוד צודק.
2. **`npm run tauri dev` להרצה, אף פעם לא `cargo run` לבד.** הרצת צד ה-Rust לבדו עוקפת את `strictPort` והחלון נטען מול שרת פיתוח של פרויקט אחר.
3. **חבילת בדיקות שדולגה מדווחת כמו חבילה שנכשלה.** ״הכול ירוק״ חייב תמיד להגיד את אותו דבר.
4. **המנוע מחזיר קודים, לא משפטים.** הניסוח בשתי השפות שייך לחלון, ו-`check-i18n` אוכף.
5. **כל מחרוזת שהמשתמש רואה — בשתי חבילות ההודעות.** RTL לוגי (`ms-`/`me-`), לא ויזואלי.
6. **רשימת היתר ולא רשימת איסור**, בכל מקום שמחליט מה מותר.
7. **`fixtures/*.pdf` לא נכנסים לריפו — הוא ציבורי והמסמכים של צד שלישי.** מסמכים אישיים של המשתמש נבדקים במבנה בלבד, ותוכנם לא נקרא.
8. **אין פורט מאזין.** התקשורת עם המנוע היא NDJSON על stdin/stdout.

## הבדיקות, לפני כל טענה שמשהו הסתיים

```bash
cd desktop/app && npm run check              # גרסה, רישיונות, טיפוסים, מחרוזות, נגישות
cd desktop/app/src-tauri && cargo test --lib
cd desktop/engine && .venv/Scripts/python.exe run_suites.py
```

## תשובות

בסגנון `codex-style`. **חריג:** מסמכים שנכתבים לקריאה של מישהו אחר — README, אפיון, יומן הכרעות — נכתבים בסגנון המאגר.

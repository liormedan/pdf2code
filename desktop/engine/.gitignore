# The engine venv is a local build artifact, not the source of truth.
# requirements.txt is.
.venv/
__pycache__/
*.pyc

# The frozen engine — 41MB of build output, rebuilt by desktop/engine/build.py
binaries/
dist/
build/
*.spec
